name: Schedule Job

on:
  schedule:
    - cron: "0 0 * * *"  # "min hr year month day"
jobs:
  run-script:
    runs-on: ubuntu-latest
    steps:
      - name: Print Message
        run: echo "Schedule Workflow Execute"
      
